package com.student.api.mapper;

import org.springframework.stereotype.Component;

import com.student.api.entity.Student;
import com.student.api.vo.StudentVo;

@Component
public class StudentMapper {

	/*
	 * public StudentVo entityToVo(Student save) {
	 * 
	 * StudentVo studentVo =
	 * StudentVo.builder().id(save.getId()).name(save.getName()).salary(save.
	 * getSalary())
	 * .achievements(save.getAchievements()).designation(save.getDesignation())
	 * .currentWorkingProject(save.getCurrentWorkingProject()).department(save.
	 * getDepartment())
	 * .company(save.getCompany()).reportingManager(save.getReportingManager())
	 * .phoneNumber(save.getPhoneNumber()).build(); return studentVo; }
	 * 
	 * public Student voToEntity(StudentVo studentVo) {
	 * 
	 * Student employee =
	 * Student.builder().id(studentVo.getId()).name(studentVo.getName())
	 * .salary(studentVo.getSalary()).achievements(studentVo.getAchievements())
	 * .designation(studentVo.getDesignation()).currentWorkingProject(studentVo.
	 * getCurrentWorkingProject())
	 * .department(studentVo.getDepartment()).company(studentVo.getCompany())
	 * .reportingManager(studentVo.getReportingManager()).phoneNumber(studentVo.
	 * getPhoneNumber()).build(); return employee; }
	 */
}
